---
import BaseLayout from '@layouts/BaseLayout.astro';
import Header from '@components/Header.astro';
import Sidebar from '@components/Sidebar.astro';
import Breadcrumb from '@components/Breadcrumb.astro';

export function getStaticPaths() {
  return [
    { params: { country: 'ethiopia' } },
    { params: { country: 'uganda' } },
    { params: { country: 'kenya' } },
    { params: { country: 'tanzania' } },
    { params: { country: 'rwanda' } },
    { params: { country: 'south-sudan' } },
  ];
}

const { country } = Astro.params;
const currentPath = Astro.url.pathname;

const countryNames = {
  ethiopia: 'Ethiopia',
  uganda: 'Uganda',
  kenya: 'Kenya'
};

const items = [
  {
    title: 'Foundation Industries',
    desc: 'Basic industries that support the entire economy',
    icon: '🏗️',
    href: `/invest/en/${country}/industry/foundation`,
    color: '#f59e0b'
  },
  {
    title: 'Artery Industries',
    desc: 'Circulation industries connecting all sectors',
    icon: '🛤️',
    href: `/invest/en/${country}/industry/artery`,
    color: '#3b82f6'
  },
  {
    title: 'Finance & Distribution',
    desc: 'Capital allocation and wealth distribution',
    icon: '🏦',
    href: `/invest/en/${country}/industry/finance`,
    color: '#10b981'
  },
  {
    title: 'Domestic Industries',
    desc: 'Consumer industries serving local markets',
    icon: '🏪',
    href: `/invest/en/${country}/industry/domestic`,
    color: '#ef4444'
  },
  {
    title: 'Arbitrage Industries',
    desc: 'Trade industries leveraging domestic-international price differences',
    icon: '⚖️',
    href: `/invest/en/${country}/industry/arbitrage`,
    color: '#8b5cf6'
  },
];
---

<BaseLayout title={`Industry - ${countryNames[country]}`}>
  <Header country={country} />
  
  <div class="flex">
    <Sidebar currentPath={currentPath} country={country} />
    
    <main class="flex-1 p-6 max-w-5xl">
      <Breadcrumb items={[{ name: countryNames[country], href: `/invest/en/${country}` }, { name: 'Industry' }]} />
      
      <div class="mb-8">
        <h1 class="text-2xl font-bold mb-2">🏭 Industry Deep Dive</h1>
        <p class="text-sm" style="color: var(--text-secondary)">Analyze investment opportunities and risks across {countryNames[country]}'s industries</p>
      </div>

      <div class="grid md:grid-cols-2 gap-4">
        {items.map(item => (
          <a 
            href={item.href} 
            class="group block p-6 rounded-xl transition-all hover:scale-[1.02] hover:-translate-y-1"
            style="background: var(--bg-card); border: 1px solid var(--border-color);"
          >
            <div class="flex items-start space-x-4">
              <div 
                class="w-14 h-14 rounded-xl flex items-center justify-center text-2xl flex-shrink-0"
                style={`background: ${item.color}20;`}
              >
                {item.icon}
              </div>
              <div class="flex-1">
                <h2 class="text-lg font-bold mb-1 group-hover:underline" style="color: var(--text-primary);">
                  {item.title}
                </h2>
                <p class="text-sm" style="color: var(--text-secondary);">{item.desc}</p>
              </div>
              <svg class="w-5 h-5 opacity-0 group-hover:opacity-100 transition-opacity" fill="none" stroke="currentColor" viewBox="0 0 24 24" style="color: var(--accent-blue);">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
              </svg>
            </div>
          </a>
        ))}
      </div>
    </main>
  </div>
</BaseLayout>
